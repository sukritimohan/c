#include <stdio.h>
#include <stdlib.h>

int main()
{
int i,j;
for(i=2;i<=12;i+=2)
{
    for(j=0;j<i;j++)
    {
        if(j%2==0)
            printf("%d",0);
        else
            printf("%d",1);
    }
    printf("\n");
}
    return 0;
}
